import { FormatNumberOptions } from "react-intl";

export const FORMAT_NUMBER_MAKER_TAKER_OPTIONS: FormatNumberOptions = {
	useGrouping: false,
	maximumFractionDigits: 4,
	minimumFractionDigits: 4,
};

export const MIN_ALP_AMOUNT = 10;
